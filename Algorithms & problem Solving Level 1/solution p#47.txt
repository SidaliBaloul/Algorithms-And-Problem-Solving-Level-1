int ReadNumber(string msg)
{
    int num;
    do
    {
        cout << msg << endl;
        cin >> num;
    } while (num <= 0);

    return num;
}
int Calculate(int loan, int pay)
{
    return (float)loan / pay;
}


int main()
{
    float LoanAmount = ReadNumber("enter the loan : ");
    float MonthlyPay = ReadNumber("enter the monthly pay : ");

    cout << Calculate(LoanAmount, MonthlyPay) << " months " << endl;