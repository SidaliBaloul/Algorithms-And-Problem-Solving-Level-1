struct stRequirments
{
    int age;
    bool HasDriverl;
    bool Hasrecom;
};

stRequirments ReadInfos()
{
    stRequirments infos;
    cout << "enter ur age : " << endl;
    cin >> infos.age;
    cout << "enter dl : " << endl;
    cin >> infos.HasDriverl;
    cout << "enter recom " << endl;
    cin >> infos.Hasrecom;

    return infos;
}

bool Isaccepted(stRequirments infos)
{
    if (infos.Hasrecom)
    {
        return true;
    }
    else
    {
       return (infos.age > 21 && infos.HasDriverl == 1);
    }
    
}

void PrintResults(stRequirments infos)
{
    if (Isaccepted(infos))
    {
        cout << "hired" << endl;
    }
    else
    {
        cout << "rejected" << endl;
    }
}

int main()
{
    PrintResults(ReadInfos());