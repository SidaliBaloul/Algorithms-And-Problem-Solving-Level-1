float ReadNumber()
{
    float r;
    cout << "enter r value : " << endl;
    cin >> r;
    return r;
}

float CalculateResult(float r)
{
    const float PI = 3.14;
    float area = PI * pow(r, 2);
    return area;
}
void PrintResult(float result)
{
    cout << "total is : " << result << endl;
}

int main()
{
    
    PrintResult(CalculateResult(ReadNumber()));