float ReadNumber()
{
    float l;
    cout << "enter the l value : " << endl;
    cin >> l;
    return l;
}

float Calculatecirclearea(float l)
{
    const float PI = 3.14;
    float area = (pow(l, 2)) / (4 * PI);
    return area;
}

void PrintResult(float result)
{
    cout << "result : " << result << endl;
}

int main()
{
    PrintResult(Calculatecirclearea(ReadNumber()));